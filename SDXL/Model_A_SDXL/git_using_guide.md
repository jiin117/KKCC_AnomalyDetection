먼저 새로운 브랜치를 만든다 => git branch 브랜치이름
새로 만든 브랜치로 이동한다. => git switch 브랜치이름

코드를 수정하거나, 새로운 코드를 작성한다.
수정한 것들을 브랜치에 올린다.
특정 파일만 추가 => git add 파일명
현재 폴더의 모든 변경 추가 => git add .
프로젝트의 모든 변경 추가 => git add -A

변화를 저장한다.(커밋한다.) => git commit -m "메시지 적기, 변경 내용 설명 등"